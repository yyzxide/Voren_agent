# Copy values into your shell environment or an ignored .env loader.
# Never commit a real API key.
OPENAI_API_KEY=replace-me
VOREN_MODEL=replace-with-supported-model-id
# OPENAI_BASE_URL=https://api.openai.com/v1
